export type VehicleAttribute = {
    id: string
    name: string
}

export type VehicleBody = VehicleAttribute

export type VehicleDrivingWheel = VehicleAttribute

export type VehicleEngineType = {
    id: string
    name: string
}

export type VehicleSteeringWheel = {
    id: string
    name: string
}

export type VehicleTransmission = {
    id: string
    name: string
}
