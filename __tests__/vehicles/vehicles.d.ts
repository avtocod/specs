export type VehicleMark = {
    id: string
    name: string
}

export type VehicleModel = {
    id: string
    name: string
    mark_id: string
}

export type VehicleType = {
    id: string
    name: string
    alias: string
}
